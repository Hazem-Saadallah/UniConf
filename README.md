# UniConf 
A simple unified interface to multiple file formats used at configuration files (e.g., TOML, YAML, XML, JSON, etc.)
The project uses external parsers and does not implement any parser from scratch.
Note: Currently, only the TOML interface is implemented.
